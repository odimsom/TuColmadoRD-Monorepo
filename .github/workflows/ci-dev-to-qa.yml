name: CI - Dev → QA (DESACTIVADO — migrado a Jenkins)

# Desactivado: Jenkins maneja este pipeline via Jenkinsfile (rama dev)
# Para reactivar temporalmente cambia workflow_dispatch por push: branches: [dev]
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  _disabled:
    name: Este workflow está desactivado
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD migrado a Jenkins. Ver Jenkinsfile en la raíz del repo."
